name: it-service-catalogue

corpus:
  id_field: service_id
  group_field: category_id
  name_field: title
  description_field: summary

pipeline:
  min_score_threshold: 0.35
  top_k: 3
  domain_description: "an IT helpdesk user"

storage:
  mode: local
  local_output_dir: output/
