# Override these env vars to point at pipeline output for local dev.
# Production: override to S3-fetched paths.
search.model.path=${SEARCH_MODEL_PATH:src/main/resources/model/autosearch-embed.onnx}
search.tokenizer.path=${SEARCH_TOKENIZER_PATH:src/main/resources/model/tokenizer.json}
search.embeddings.path=${SEARCH_EMBEDDINGS_PATH:src/main/resources/embeddings/data-items.json}
server.port=8080
